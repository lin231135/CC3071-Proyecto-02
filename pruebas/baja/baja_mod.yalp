/* Parser Baja MODIFICADO: asignaciones con suma y resta */

/* ── Sección de TOKENS ── */
%token ID NUM PLUS MINUS ASSIGN SEMI WS
IGNORE WS

%%

/* ── Sección de PRODUCCIONES ── */
programa:
    lista_stmt
;
lista_stmt:
    lista_stmt stmt
  | stmt
;
stmt:
    ID ASSIGN expr SEMI
;
expr:
    expr PLUS term
  | expr MINUS term
  | term
;
term:
    ID
  | NUM
;
